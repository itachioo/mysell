<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.study.sell.mysell.mapper.OrderInfoMapper">
    <insert id="insert" parameterType="com.study.sell.mysell.entity.OrderInfo">
        INSERT INTO order_info (order_id, buyer_name, buyer_phone, address, order_status, buyer_openid, pay_status, order_amount)
        VALUES (#{orderId}, #{buyerName}, #{buyerPhone},#{address},#{orderStatus},#{buyerOpenid},#{payStatus},#{orderAmount})
    </insert>

</mapper>